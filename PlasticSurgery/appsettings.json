{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "Postgres": ""
  },
  "App": {
    "_comment": "PublicBaseUrl = the externally reachable HTTPS origin of this app (no trailing slash). Telegram webhooks are registered as {PublicBaseUrl}/api/integrations/telegram/webhook/{connectionId}. Required for local testing (use an https tunnel URL); on Render set App__PublicBaseUrl. If empty, the current request's host is used unless it is localhost.",
    "PublicBaseUrl": ""
  },
  "Meta": {
    "AppId": "1779862623023059",
    "GraphApiVersion": "v21.0",
    "FacebookLoginConfigId": "2356719648479257",
    "WhatsAppLoginConfigId": "1811288343627090"
  },
  "Embeddings": {
    "_comment": "Knowledge Base embeddings (OpenAI-compatible /embeddings). ApiKey is a secret — set it via user-secrets or the Embeddings__ApiKey env var, never here. Dimensions must equal the vector(N) size of knowledge_chunks.embedding in Database/schema.sql.",
    "BaseUrl": "https://api.openai.com/v1",
    "Model": "text-embedding-3-small",
    "Dimensions": 1536,
    "ApiKey": ""
  },
  "Knowledge": {
    "_comment": "DEFAULTS ONLY — used to create a clinic's knowledge_search_settings row the first time it's needed (chunk chars ÷ 4 = tokens). After that the clinic's persisted settings (Knowledge Base → Settings) are what's used. ChunkMinChars is a system-wide value not stored per clinic.",
    "MinScore": 0.30,
    "ChunkMaxChars": 1000,
    "ChunkOverlapChars": 150,
    "ChunkMinChars": 200,
    "MaxUploadBytes": 5242880,
    "MaxExtractedChars": 250000,
    "WebScraping": {
      "_comment": "Website crawler limits (Knowledge Base -> Websites). All are clamped in code. DevAllowedHosts (exact host:port entries that bypass the SSRF rules) is honoured ONLY when ASPNETCORE_ENVIRONMENT=Development — leave it empty.",
      "MaxPages": 100,
      "MaxDepth": 3,
      "MaxConcurrency": 3,
      "RequestTimeoutSeconds": 15,
      "MaxResponseBytes": 2000000,
      "MaxRedirects": 5,
      "PolitenessDelayMs": 300,
      "MaxRunMinutes": 20,
      "MinTextChars": 80,
      "MaxTextChars": 200000,
      "MaxQueryVariantsPerPath": 5,
      "MaxLinksPerPage": 300,
      "MaxSourcesPerClinic": 25,
      "DevAllowedHosts": []
    }
  },
  "N8n": {
    "_comment": "Shared secret n8n must send as the X-Ingest-Key header on POST /api/messages/ingest. Keep the real value out of this file (checked into the repo) — set it via `dotnet user-secrets set \"N8n:IngestApiKey\" \"...\"` instead; user-secrets overrides whatever is here.",
    "IngestApiKey": "",
    "_comment2": "KnowledgeBenchmarkWebhookUrl = the production webhook URL of the SEPARATE n8n workflow that writes patient-style questions for the Knowledge Retrieval Benchmark (Knowledge Base -> Benchmark -> Generate Test Cases). Not the patient AI workflow. Set it via the N8n__KnowledgeBenchmarkWebhookUrl env var / user-secrets; empty disables question generation (cases can still be added by hand).",
    "KnowledgeBenchmarkWebhookUrl": ""
  }
}
