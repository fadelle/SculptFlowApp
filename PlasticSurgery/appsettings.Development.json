{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore": "Information"
    }
  },

  "_comment": "Fill in ConnectionStrings:Postgres before running — see Database/README.md. Better yet, keep the real value out of this file (it's checked into the repo) and use `dotnet user-secrets set \"ConnectionStrings:Postgres\" \"...\"` from the PlasticSurgery project folder instead; user-secrets overrides whatever is here.",

  "ConnectionStrings": {
    "Postgres": "Host=localhost;Port=5432;Database=plasticsurgery;Username=REPLACE_ME;Password=REPLACE_ME"
  }
}
